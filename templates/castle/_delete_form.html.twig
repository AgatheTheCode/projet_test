<form method="post" action="{{ path('app_castle_delete', {'id': castle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ castle.id) }}">
    <button class="btn">Delete</button>
</form>
